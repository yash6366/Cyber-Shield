import React from 'react';

function AttackSimulationWorkspace() {
  return (
    <div>
      <h2>Attack Simulation Workspace</h2>
      <p>Sandbox environment for agents to predict and counter simulated cyberattacks with live event logs.</p>
    </div>
  );
}

export default AttackSimulationWorkspace;
